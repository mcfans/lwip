[package]
name = "linux_tun_example"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tokio = { version = "1.11.0", features = ["rt", "net", "rt-multi-thread"] }
tun = { path = "../" }

[build-dependencies]
cc = { version = "1.0", features = ["parallel"] }
